ARG BASE_IMAGE=kulnor/fair-data-machine:dev
FROM ${BASE_IMAGE}

# Nginx Proxy Manager: reverse proxy and TLS management utility.
# Placeholder — full integration pending deployment architecture decision.
# NPM requires Node.js, a database backend (SQLite or MySQL), and its web application stack.
# See: https://nginxproxymanager.com/setup/ for the full installation procedure.

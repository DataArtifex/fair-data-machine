[project]
name = "fair-data-machine-builder"
version = "0.1.0"
description = "UI and Generator for creating custom FAIR Data Machine Docker images"
requires-python = ">=3.12"
dependencies = ["gradio>=4.0.0"]

[tool.uv]
managed = true
dev-dependencies = []
